
services:
  - type: web
    name: legal-library-backend
    env: node
    plan: free
    buildCommand: "cd backend && npm install"
    startCommand: "node backend/server.js"
    repo: https://your-github-repo-url
    branch: main
    rootDir: /
